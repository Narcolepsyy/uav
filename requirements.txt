# Core (CPU/dev); Jetson aarch64 will skip torch/torchvision via markers
torch>=2.1.0; platform_machine != "aarch64"
torchvision>=0.16.0; platform_machine != "aarch64"
timm>=0.9.2
numpy>=1.24.0
opencv-python>=4.8.0; platform_machine != "aarch64"

# Training & Utilities
pyyaml>=6.0.1
tqdm>=4.66.1
matplotlib>=3.7.0

# Evaluation/metrics
pycocotools>=2.0.7; platform_system != "Windows"

# ONNX export & runtime (skip runtime on Jetson)
onnx>=1.14.0
onnxruntime>=1.16.0; platform_machine != "aarch64"

# TensorRT deployment (install manually on Jetson via NVIDIA SDK Manager)
# tensorrt==8.*  # Do NOT install via pip on Jetson Nano; use NVIDIA-provided packages

# Logging & profiling
pandas>=2.0.0
psutil>=5.9.5

# Notes for NVIDIA Jetson Nano (aarch64):
# - Install torch/torchvision using NVIDIA's prebuilt wheels (JetPack/CUDA compatible).
# - Prefer system OpenCV from apt over pip opencv-python to avoid conflicts.